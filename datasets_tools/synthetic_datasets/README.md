## Data Preprocessing

1. We have provided preprocessed dataset in the `./data/lorenz_data/25052216`.
2. If you want to re-generate the data and process it, use the following steps

```bash
cd ROOT_DIR
sh datasets_tools/synthetic_datasets/gen_lorenz.sh
```
